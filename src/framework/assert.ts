// TODO: enable this once we're on new enough TypeScript (3.7.x) and Babel (7.7.x?).

//export function assert(condition: any, msg?: string): asserts condition {
//  if (!condition) {
//      throw new Error(msg);
//  }
//}
